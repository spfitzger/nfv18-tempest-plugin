# Intentionally empty.
#
# pbr maps this file to install_requires. When test-operator installs the
# plugin via externalPlugin (`pip install .` in the tempest pod), any pins
# here force pip to contact a package index. Lab egress often returns an
# Anubis/bot-check HTML page instead of package metadata, which pip then
# mis-parses as a PEP 508 marker (InvalidMarker: "re not a bot!...").
#
# RHOSO openstack-tempest-all already provides tempest, clients, paramiko,
# requests, etc. Register the plugin only; do not re-resolve deps at runtime.
#
# For local development: pip install -r requirements-dev.txt && pip install -e .
